apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy

providers:
  - name: demo
    orgId: 1
    folder: Demo
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
